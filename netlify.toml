[build]
  command = "npm run build"
  publish = "dist"
  # functions = "netlify/functions"  # Hope Assistant disabled — uncomment to re-enable

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
